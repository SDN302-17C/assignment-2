<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List of Quizzes</title>
</head>
<body>
    <h1>List of quizzes</h1>
    <a href="/quizzes/new">Create New quiz</a>
    <ul>
        <% quizzes.forEach(quiz => { %>
            <li>
                <a href="/quizzes/<%= quiz._id %>"><%= quiz.title %></a>
                <a href="/quizzes/<%= quiz._id %>/edit">Edit</a>
                <form action="/quizzes/<%= quiz._id %>?_method=DELETE" method="POST" style="display:inline;">
                    <button type="submit">Delete</button>
                </form>
            </li>
        <% }); %>
    </ul>
</body>
</html>